<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Friends</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f5f5f5; /* Light mode background */
            font-family: 'Poppins', sans-serif;
            margin: 0;
        }

        /* Wrapper to center everything */
        .friends-wrapper {
            width: 90%;
            max-width: 400px;
            background: #fff; /* White card */
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        /* Title */
        .friends-title {
            text-align: center;
            font-size: 22px;
            color: #333;
            margin-bottom: 15px;
        }

        /* Friends List */
        .friends-list {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        /* Friend Card */
        .friend-card {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px;
            background: #f0f0f0;
            border-radius: 10px;
            transition: background 0.3s ease-in-out;
        }

        /* Friend Card Hover */
        .friend-card:hover {
            background: #e0e0e0;
        }

        /* Friend Info (Avatar + Name) */
        .friend-info {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        /* Avatar */
        .friend-avatar {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 40px;
            height: 40px;
            background: #007bff;
            color: white;
            font-weight: bold;
            font-size: 18px;
            border-radius: 50%;
        }

        /* Friend Name */
        .friend-name {
            font-size: 16px;
            color: #333;
        }

        /* Chat Icon Button */
        .chat-icon {
            font-size: 20px;
            text-decoration: none;
            color: #007bff;
            transition: transform 0.2s;
        }

        /* Chat Icon Hover */
        .chat-icon:hover {
            transform: scale(1.2);
        }

    </style>
</head>
<body>
    <div class="friends-wrapper">
        <h1 class="friends-title">Your Friends</h1>
        <div class="friends-list">
            <% friends.forEach(element => { %>
                <div class="friend-card">
                    <div class="friend-info">
                        <span class="friend-avatar"><%= element.Username.charAt(0) %></span>
                        <span class="friend-name"><%= element.Username %></span>
                    </div>
                    <a href="/messages/<%= element.id %>" class="chat-icon">💬</a>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>